$(document).ready(function() {
	
	/*
	var video = document.getElementById('video-intro');
	video.pause();

	setInterval(function(){
		video.currentTime = window.pageYOffset/400;
		var position = video.currentTime >= video.duration ? 'absolute' : 'fixed';
		var top = video.currentTime >= video.duration ? 'inherit' : '0';
		$('#video-intro, #video-title').css({'position': position, 'top': top});
		$('#video-title').css({
			'opacity': video.duration - (video.currentTime * 2),
			'margin-top': 200 - video.currentTime * 10
		});
		video.pause();
	}, 40);
	*/
});